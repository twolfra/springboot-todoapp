spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  flyway:
    enabled: false   # disable migrations for tests (optional)

jwt:
  secret: "fN3KueiixLXNbgLSYsHWt3gWAbvZwYFy"
